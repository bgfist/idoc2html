/** 调试配置 */
export const debug = {
    sizeSpec: false,
    buildPreOnly: false,
    buildAllNodes: false,
};

/** 默认配置 */
export let defaultConfig = {
    codeGenOptions: {
        role: true,
        experimentalZIndex: false
    },
};